// TODO Dev 1 — Sprint 1 | Issue #1
// Implementar MaxHeap para inventário de pistas
//
// Cada pista: { id: string, nome: string, peso: number (1–10), descricao: string }
//
// Métodos obrigatórios:
//   insert(pista)   → insere e mantém propriedade do heap       O(log n)
//   extractMax()    → remove e retorna o elemento de maior peso O(log n)
//   top3()          → retorna as 3 pistas de maior peso          O(n log n)
//   Heap vazio deve retornar null sem lançar exceção.
//
// Integração: usado por partidaController → endpoint GET /api/partida/:id/inventario

class MaxHeap {
  constructor() {
    // TODO
  }

  insert(pista) {
    // TODO
  }

  extractMax() {
    // TODO
  }

  top3() {
    // TODO
  }
}

module.exports = MaxHeap;
